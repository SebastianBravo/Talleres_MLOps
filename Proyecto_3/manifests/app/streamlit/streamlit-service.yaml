apiVersion: v1
kind: Service
metadata:
  name: streamlit
  namespace: mlops-app
  labels:
    app: streamlit
spec:
  type: NodePort
  ports:
    - name: "8501"
      port: 8501
      targetPort: 8501
      nodePort: 30501
  selector:
    app: streamlit
