apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: mlops-obs
  labels:
    app: grafana
spec:
  ports:
    - name: "3000"
      port: 3000
      targetPort: 3000
  selector:
    app: grafana
