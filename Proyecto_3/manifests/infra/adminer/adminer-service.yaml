apiVersion: v1
kind: Service
metadata:
  name: adminer
  namespace: mlops-infra
  labels:
    app: adminer
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: adminer
