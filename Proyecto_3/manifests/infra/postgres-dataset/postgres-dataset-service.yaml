apiVersion: v1
kind: Service
metadata:
  name: postgres-dataset
  namespace: mlops-infra
  labels:
    app: postgres-dataset
spec:
  ports:
    - name: "5432"
      port: 5432
      targetPort: 5432
  selector:
    app: postgres-dataset
