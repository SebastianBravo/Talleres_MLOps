# API de datos (Covertype) para pruebas locales.
# Sirve datos por batches; el DAG de Airflow se conecta a este servicio como api-data.

services:
  api-data:
    build: ../data-api
    ports:
      - "8082:80"
    volumes:
      - ../data-api/data:/data
      - ../data-api/main.py:/main.py
    restart: always
